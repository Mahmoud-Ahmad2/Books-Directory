import {insert, getBook, deleteBook, update } from './book'
import { getByEmail, createUser } from './users';

export {
  insert,
  getBook,
  deleteBook,
  update,
  getByEmail,
  createUser,
}